<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WatchTower MFG</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background: #0a0a0a;
            color: #e0e0e0;
            overflow-x: hidden;
        }
        header {
            background: linear-gradient(90deg, #1e1e1e, #2c2c2c);
            padding: 20px;
            text-align: center;
            border-bottom: 2px solid #00ffcc;
            box-shadow: 0 4px 10px rgba(0, 255, 204, 0.2);
        }
        header h1 {
            margin: 0;
            font-size: 2.5em;
            color: #00ffcc;
            text-shadow: 0 0 10px rgba(0, 255, 204, 0.5);
        }
        nav {
            display: flex;
            justify-content: center;
            gap: 15px;
            padding: 15px;
            background: #1e1e1e;
        }
        nav button {
            background: #333;
            border: 1px solid #00ffcc;
            color: #00ffcc;
            padding: 10px 20px;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        nav button:hover, nav button.active {
            background: #00ffcc;
            color: #0a0a0a;
            box-shadow: 0 0 15px rgba(0, 255, 204, 0.7);
        }
        main {
            max-width: 800px;
            margin: 30px auto;
            padding: 20px;
            background: #1e1e1e;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 255, 204, 0.1);
            min-height: 500px;
            transition: all 0.5s ease;
        }
        .task-section {
            display: none;
            padding: 20px;
        }
        .task-section.active {
            display: block;
        }
        .upload-section {
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 10px;
            font-weight: bold;
            color: #00ffcc;
        }
        input[type="file"] {
            width: 100%;
            padding: 10px;
            background: #333;
            border: 1px solid #00ffcc;
            border-radius: 5px;
            color: #e0e0e0;
        }
        .button-group button {
            background: #00ffcc;
            color: #0a0a0a;
            border: none;
            padding: 10px 20px;
            border-radius: 25px;
            cursor: pointer;
            margin-right: 10px;
            transition: all 0.3s ease;
        }
        .button-group button:hover {
            background: #00cc99;
            box-shadow: 0 0 10px rgba(0, 255, 204, 0.5);
        }
        .results img {
            max-width: 100%;
            border-radius: 10px;
            margin-top: 15px;
            border: 1px solid #00ffcc;
        }
        .results div {
            margin-top: 15px;
            padding: 15px;
            background: #2c2c2c;
            border-radius: 10px;
            color: #e0e0e0;
            border: 1px solid #00ffcc;
        }
        footer {
            text-align: center;
            padding: 15px;
            background: #1e1e1e;
            color: #00ffcc;
            position: fixed;
            bottom: 0;
            width: 100%;
            border-top: 1px solid #00ffcc;
        }
    </style>
</head>
<body>
    <header>
        <h1>WatchTower MFG</h1>
    </header>
    <nav>
        <button onclick="showTask('object')" class="active">Object Detection</button>
        <button onclick="showTask('classify')">Image Classification</button>
        <button onclick="showTask('anomaly')">Anomaly Detection</button>
        <button onclick="showTask('ocr')">OCR</button>
    </nav>
    <main>
        <section id="object" class="task-section active">
            <h2>Object Detection</h2>
            <div class="upload-section">
                <label for="objectUpload">Upload Image:</label>
                <input type="file" id="objectUpload" accept="image/*">
                <div class="button-group">
                    <button onclick="previewImage('object')">Preview</button>
                    <button onclick="runInspection('object')">Run Detection</button>
                </div>
            </div>
            <div class="results">
                <img id="objectPreview" src="" alt="Object Detection Preview" style="display: none;">
                <div id="objectResult">Results will appear here.</div>
            </div>
        </section>

        <section id="classify" class="task-section">
            <h2>Image Classification</h2>
            <div class="upload-section">
                <label for="classifyUpload">Upload Image:</label>
                <input type="file" id="classifyUpload" accept="image/*">
                <div class="button-group">
                    <button onclick="previewImage('classify')">Preview</button>
                    <button onclick="runInspection('classify')">Run Classification</button>
                </div>
            </div>
            <div class="results">
                <img id="classifyPreview" src="" alt="Classification Preview" style="display: none;">
                <div id="classifyResult">Results will appear here.</div>
            </div>
        </section>

        <section id="anomaly" class="task-section">
            <h2>Anomaly Detection</h2>
            <div class="upload-section">
                <label for="anomalyUpload">Upload Image:</label>
                <input type="file" id="anomalyUpload" accept="image/*">
                <div class="button-group">
                    <button onclick="previewImage('anomaly')">Preview</button>
                    <button onclick="runInspection('anomaly')">Run Detection</button>
                </div>
            </div>
            <div class="results">
                <img id="anomalyPreview" src="" alt="Anomaly Detection Preview" style="display: none;">
                <div id="anomalyResult">Results will appear here.</div>
            </div>
        </section>

        <section id="ocr" class="task-section">
            <h2>OCR (Text Recognition)</h2>
            <div class="upload-section">
                <label for="ocrUpload">Upload Image:</label>
                <input type="file" id="ocrUpload" accept="image/*">
                <div class="button-group">
                    <button onclick="previewImage('ocr')">Preview</button>
                    <button onclick="runInspection('ocr')">Run OCR</button>
                </div>
            </div>
            <div class="results">
                <img id="ocrPreview" src="" alt="OCR Preview" style="display: none;">
                <div id="ocrResult">Results will appear here.</div>
            </div>
        </section>
    </main>
    <footer>
        <p>© 2025 WatchTower MFG</p>
    </footer>

    <script>
        function showTask(task) {
            document.querySelectorAll('.task-section').forEach(section => {
                section.classList.remove('active');
            });
            document.getElementById(task).classList.add('active');
            document.querySelectorAll('nav button').forEach(btn => {
                btn.classList.remove('active');
            });
            document.querySelector(`nav button[onclick="showTask('${task}')"]`).classList.add('active');
        }

        function previewImage(type) {
            const fileInput = document.getElementById(`${type}Upload`);
            const preview = document.getElementById(`${type}Preview`);
            const file = fileInput.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    preview.src = e.target.result;
                    preview.style.display = 'block';
                };
                reader.readAsDataURL(file);
            }
        }

        function runInspection(type) {
            const fileInput = document.getElementById(`${type}Upload`);
            const resultText = document.getElementById(`${type}Result`);
            const preview = document.getElementById(`${type}Preview`);
            const file = fileInput.files[0];

            if (!file) {
                resultText.textContent = "Please upload an image first.";
                return;
            }

            const formData = new FormData();
            formData.append('image', file);
            formData.append('task_type', type);

            fetch('http://localhost:5000/inspect', {
                method: 'POST',
                body: formData
            })
            .then(response => {
                if (!response.ok) throw new Error('Network response was not ok');
                return response.json();
            })
            .then(data => {
                if (type === 'object' && data.annotated_image) {
                    preview.src = data.annotated_image;
                    preview.style.display = 'block';
                    resultText.textContent = data.detections.map(d => `${d.label}: ${d.score}`).join(', ');
                } else {
                    resultText.textContent = data.label || data.result || data.text || data.error;
                    preview.style.display = 'block';
                }
            })
            .catch(error => {
                resultText.textContent = "Error: Could not connect to server or process image.";
                console.error(error);
            });
        }
    </script>
</body>
</html>